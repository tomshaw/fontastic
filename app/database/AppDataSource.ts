// import { DataSource, DataSourceOptions } from "typeorm"
// import { Collection, Store, User, Logger } from "../database/entity";

// const schemas = [Collection, Store, User, Logger];

/*
let exampleConnections = {
  sqlite: {
    type: "sqlite",
    synchronize: true,
    database: "fontastic.sqlite",
    entities: schemas,
    logger: "simple-console",
    logging: true
  },
  mysql: {
    type: "mysql",
    host: "localhost",
    port: 3306,
    username: "root",
    password: "password",
    database: "project_fontastic",
    entities: schemas,
    synchronize: true,
    logger: "simple-console",
    logging: true
  }
}
*/

// const AppDataSource = new DataSource({
//   type: "sqlite",
//   synchronize: true,
//   database: "fontastic.sqlite",
//   entities: schemas,
//   logger: "simple-console",
//   logging: true
// });

// export default AppDataSource;