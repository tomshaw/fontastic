import { InstallablePipe } from './installable.pipe';

describe('InstallablePipe', () => {
  it('create an instance', () => {
    const pipe = new InstallablePipe();
    expect(pipe).toBeTruthy();
  });
});
